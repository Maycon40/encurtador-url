{
  "name": "encurtador-url",
  "version": "1.0.0",
  "description": "Projeto para encurtar a url",
  "scripts": {
    "server": "npm run database:up && sleep 5 && npm run dev",
    "dev": "node server.js",
    "start": "node server.js",
    "test": "jest --watch",
    "database:up": "docker compose up -d",
    "database:down": "docker compose down"
  },
  "author": "Maycon",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "supertest": "^7.1.4"
  }
}